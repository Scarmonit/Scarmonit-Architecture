{
  "name": "scarmonit-architecture",
  "version": "2.5.0",
  "description": "Unified AI Infrastructure Architecture - Autonomous agents, edge computing, and premium UI",
  "private": true,
  "workspaces": [
    "web-portal",
    "agent-api",
    "desktop-app"
  ],
  "scripts": {
    "install:all": "npm install && cd agent-api && npm install && cd .. && cd desktop-app && npm install && cd ..",
    "dev:web": "cd web-portal && python -m http.server 8080",
    "dev:api": "cd agent-api && npx wrangler dev",
    "dev:desktop": "cd desktop-app && npm start",
    "deploy:web": "cd web-portal && npx wrangler pages deploy . --project-name=scarmonit-www",
    "deploy:api": "cd agent-api && npx wrangler deploy --env production",
    "deploy:all": "npm run deploy:web && npm run deploy:api",
    "test": "echo Tests coming soon",
    "lint": "echo Linting coming soon"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Scarmonit/Scarmonit-Architecture.git"
  },
  "keywords": [
    "ai",
    "agents",
    "cloudflare",
    "llm",
    "infrastructure",
    "automation",
    "edge-computing"
  ],
  "author": "Scarmonit <scarmonit@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
