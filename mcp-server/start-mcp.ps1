# Start MCP Server for JetBrains Integration
node index.js
# Run the server

Write-Host "Press Ctrl+C to stop" -ForegroundColor Yellow
Write-Host "🔌 Server running on stdio transport..." -ForegroundColor Green
Write-Host ""
Write-Host "   - diagnose_agents" -ForegroundColor White
Write-Host "   - apply_agent_context" -ForegroundColor White
Write-Host "   - search_agents" -ForegroundColor White
Write-Host "   - get_agent_instructions" -ForegroundColor White
Write-Host "   - list_agents" -ForegroundColor White
Write-Host "   - check_system_status" -ForegroundColor White
Write-Host "   - check_datalore_status" -ForegroundColor White
Write-Host "📡 Available tools:" -ForegroundColor Cyan
Write-Host "✅ MCP Server started successfully!" -ForegroundColor Green
# Start the MCP server

}
    npm install
    Write-Host "📦 Installing dependencies..." -ForegroundColor Yellow
if (-not (Test-Path "node_modules")) {
# Check if node_modules exists

Set-Location "C:\Users\scarm\IdeaProjects\Scarmonit-Architecture\mcp-server"
# Change to MCP server directory

$env:NODE_ENV = "development"
$env:DATALORE_LICENSE_ID = "DSVYH9Q8VG"
# Set environment variables

Write-Host "🚀 Starting Scarmonit MCP Server..." -ForegroundColor Cyan

$ErrorActionPreference = "Stop"

# This script starts the Scarmonit MCP server with proper environment variables

